#!/bin/sh
# Gradle wrapper
exec "${JAVA_HOME:-$(dirname "$(command -v java)")/../..}/../bin/java" \
  -classpath "$(dirname "$0")/gradle/wrapper/gradle-wrapper.jar" \
  org.gradle.wrapper.GradleWrapperMain "$@"
